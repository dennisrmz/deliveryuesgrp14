<?xml version="1.0" encoding="utf-8"?>
<TableLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="fill_parent"
    android:layout_height="wrap_content"
    android:paddingTop="10dip"
    android:stretchColumns="1" >
    <TableRow>
        <TextView android:text="Codigo del Menu" />
        <EditText
            android:id="@+id/codMenu"
            android:inputType="number" />
    </TableRow>
    <TableRow>
        <TextView android:text="Precio del combo" />
        <EditText
            android:id="@+id/editPrec"
            android:inputType="numberDecimal" />
    </TableRow>
    <TableRow>
        <TextView android:text="Descripcion del combo" />
        <EditText
            android:id="@+id/editDescripCombo"
            android:inputType="text"
            android:maxLength="120" />
    </TableRow>
    <TableRow>
        <TextView android:text="Codigo local:" />
        <Spinner
            android:id="@+id/comboLocales"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            />
    </TableRow>
    <TableRow>
        <Button
            android:onClick="actualizarMenu"
            android:text="actualizar" />
    </TableRow>
    <TableRow>
        <Button
            android:onClick="limpiarTexto"
            android:text="limpiar" />
    </TableRow>
</TableLayout>